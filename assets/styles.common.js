import { StyleSheet } from "react-native";
const colors = {
  red: '#D94432',
  white: '#ffffff',
  lightGray: '#eef0f2',
  gray: '#959495',
};

const styles = StyleSheet.create({
  textCenter: {
    textAlign: 'center',
  },
})

export {colors};
export default styles;